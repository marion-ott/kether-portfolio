type User {
  id: ID! @id
  firstName: String!
  lastName: String!
  email: String!
  phone: Int
  address: String
  city: String
  zipCode: Int
  bio: String
  works: [Work!]!
}

type Work {
  id: ID! @id
  name: String!
}
